pipeline {
    agent any

    tools {
        
        node "NodeJS"
    }

    stages {
        stage('Build') {
            steps {
               
                sh "npm install"

            }

            
        }
    }
}
