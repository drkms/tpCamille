pipeline {
    agent any

    tools {
        // Install the Maven version configured as "M3" and add it to the path.
        node "NodeJS"
    }

    stages {
        stage('Build') {
            steps {
               
               
                sh "npm install"

            }

          
        }
    }
}
